{
  "guid": "sample-filter2",
  "name": "Sample Filter2",
  "connectors": {
    "sources": [
      { "id": "out1", "label": "out1" },
      { "id": "out2", "label": "out2" }
    ],
    "sinks": [
      { "id": "in", "label": "in" }
    ]
  },
  "code": {
    "process": "function filter(arr, predicate){ return Array.isArray(arr) ? arr.filter(predicate || (x=>x%2===0)) : arr; }"
  }
}
