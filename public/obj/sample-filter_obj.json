{
  "guid": "sample-filter",
  "name": "Sample Filter",
  "category": "Filters",
  "connectors": {
    "sources": [
      { "id": "out1", "label": "out" }
    ],
    "sinks": [
      { "id": "in", "label": "in" }
    ]
  },
  "code": {
    "process": "function filter(arr, predicate){ return Array.isArray(arr) ? arr.filter(predicate || (x=>x%2===0)) : arr; }"
  }
}
